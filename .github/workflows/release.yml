name: Release new version

on:
  workflow_dispatch:
    inputs:
      release_type:
        description: "Release type"
        required: true
        default: "patch"
        type: choice
        options:
          - major
          - minor
          - patch
          - custom
      custom_version:
        description: "Custom version (only if 'custom' is selected above)"
        required: false
        type: string
        default: ""

permissions:
  contents: write

jobs:
  release:
    uses: 4ndrs/shared-actions-testing/.github/workflows/reusable-release.yml@v1.0.1
    with:
      release_type: ${{ inputs.release_type }}
      custom_version: ${{ inputs.custom_version }}
    secrets:
      NODE_AUTH_TOKEN: ${{ secrets.NODE_AUTH_TOKEN }}
