name: Release new version

on:
  workflow_dispatch:
    inputs:
      release_type:
        description: "Release type"
        required: true
        default: "patch"
        type: choice
        options:
          - major
          - minor
          - patch
          - custom
      release_branch:
        description: "Release branch"
        required: true
        type: string
        default: ""
      custom_version:
        description: "Custom version (only if 'custom' is selected above)"
        required: false
        type: string
        default: ""

permissions:
  contents: write

jobs:
  release:
    uses: akuyaku-reijou/shared-actions-testing/.github/workflows/reusable-release.yml@v5
    with:
      release_type: ${{ inputs.release_type }}
      release_branch: ${{ inputs.release_branch }}
      custom_version: ${{ inputs.custom_version }}
      APP_ID: ${{ vars.APP_ID }}
    secrets:
      NODE_AUTH_TOKEN: ${{ secrets.NODE_AUTH_TOKEN }}
      APP_PRIVATE_KEY: ${{ secrets.APP_PRIVATE_KEY }}
